import { createMuiTheme } from '@material-ui/core/styles';
export const defaultTheme = createMuiTheme({
  palette: {
    background: {
      default: '#ede7f6',
    },
    primary: {
      main: '#A55BCE',
      light: '#dfc4ed',
    },
    text: {
      primary: '#ffffff',
    },
  },
});
